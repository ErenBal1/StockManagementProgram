package stockManagementProgram.model.enums;

/**
 * Represents the type of stock transaction in the system.
 * Used to track whether stock quantities are being increased or decreased.
 * This enum is crucial for maintaining accurate inventory records and generating reports.
 */
public enum TransactionType {
    ADDITION,
    REMOVAL
}
